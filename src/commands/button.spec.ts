import ButtonCommand from './button'
import _JSONDatabase from '../db/jsonDatabase'

describe('ButtonCommand', () => {
  it('should exist', () => {
    expect(typeof ButtonCommand).toBe('function')
  })
})
